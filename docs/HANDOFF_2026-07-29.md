# Handoff — Vide Digital (Vide Hub), sessão de 2026-07-29

Resumo objetivo pra contextualizar outra IA (qualquer uma — Claude, ChatGPT,
Codex, Gemini etc.) sem precisar reler o histórico inteiro do repositório.
Leia isto primeiro, depois `docs/ROADMAP_RD3_STATUS.md` (documento-mestre do
projeto) e `docs/WHATSAPP_OFICIAL.md` (detalhe do que está descrito abaixo).
Handoff anterior a este: `docs/HANDOFF_2026-07-28.md`.

## O que era o objetivo desta sessão

Duas missões sequenciais, ambas sobre `main` (sem PR, convenção já
estabelecida neste projeto):

1. **Auditoria Centralizada V1 — Fase B** (validação final em
   produção): Rules/índices/triggers foram publicados; ainda falta o teste
   manual real com pedido/produto de teste. Ver estado real abaixo —
   **parcial, não concluída**.
2. **WhatsApp Oficial V1 — Fase A** (código): integrar o WhatsApp
   Business Cloud API oficial da Meta na Central de Atendimento já
   existente, sem criar um segundo modelo de chat/CRM. Ver
   `docs/WHATSAPP_OFICIAL.md` para o desenho completo.

## HEAD inicial

`88c8bea` (fim da Auditoria Centralizada V1 Fase A, sessão anterior).

## Auditoria Centralizada — estado real (importante para quem continuar)

- Deploy 1/2 ("Deploy Firebase Spark": Rules/Storage/índices) —
  **publicado com sucesso** em produção (`vide-digital-saas`).
- Deploy 2/2 ("Deploy Firebase Functions — Auditoria": os 15 triggers) —
  **publicado com sucesso**, conforme confirmação operacional do usuário
  após concessão manual do IAM necessário (incluindo o service agent correto
  do Eventarc). `askBusinessAI`/`askPublicBusinessAI` permaneceram intactas.
- Auditoria Centralizada V1 continua **PARCIAL**, mas o motivo pendente não
  é mais IAM nem falta de deploy: falta o **teste manual real em produção**.
  Antes de marcar como CONCLUÍDA, ainda é obrigatório alterar um pedido e um
  produto de teste, confirmar eventos, ator/tenant, ausência de PII e
  isolamento entre tenants.

## WhatsApp Oficial V1 — Fase A: o que foi feito

Três commits atômicos, cada um verde em CI real antes do próximo:

1. `chore(functions): prepara runtime Node 22 para novos backends` +
   fix seguinte `fix(functions): corrige runtime declarado em
   firebase.json para node22` — `firebase.json` tinha
   `functions.runtime: "nodejs20"` hardcoded, que é o campo que o
   Firebase CLI realmente usa pro deploy; o bump de `engines.node` no
   `package.json` sozinho não teria efeito real. Sem redeploy de nada
   existente.
2. `feat(whatsapp): adiciona webhook e envio oficial seguro` — todo o
   backend (`functions/src/whatsapp/`), 7 Cloud Functions, coleções
   novas + Rules, scripts de piloto assistido, testes puros/Functions/
   Rules — ver `docs/WHATSAPP_OFICIAL.md` para o detalhe completo.
3. `feat(atendimento): integra WhatsApp oficial à inbox` — view de
   conexão nova + extensões pontuais na Central de Atendimento. Um bug
   real de CSS foi encontrado pelo CI real e corrigido num commit
   seguinte (`fix(atendimento): esconde de verdade o compositor fora
   da janela do WhatsApp`) — ver seção "Bug real encontrado e
   corrigido" em `docs/WHATSAPP_OFICIAL.md`.

**Playwright não pôde ser executado localmente nesta sessão**: o proxy de
rede deste ambiente de desenvolvimento bloqueia `gstatic.com` (`curl`
confirmou `403` na tunelagem CONNECT), e `firebase-init.js` carrega o SDK
do Firebase direto desse CDN — todo login real trava em `page.goto(...,
{waitUntil:"load"})`. Confirmado que **não é específico deste módulo**:
o mesmo teste-controle rodando `auditoria.flow.mjs` (já comprovado verde
em CI antes) falha exatamente do mesmo jeito neste sandbox. A validação
real de UI aconteceu só via CI (rede completa) — foi lá que o bug de CSS
acima foi encontrado e confirmado corrigido.

## Estado ao final desta sessão

- WhatsApp Oficial V1 — **Fase A concluída no código**, confirmada verde
  em CI real (4/4 jobs, incluindo o fluxo Playwright novo). Conexão real
  com a Meta depende de configuração externa — checklist completo em
  `docs/WHATSAPP_OFICIAL.md`.
- Auditoria Centralizada V1 — segue **PARCIAL**. Rules/índices e os 15
  triggers já foram publicados, conforme confirmação operacional do usuário;
  falta somente a validação manual real em produção antes de marcar como
  CONCLUÍDA.
- Nenhuma outra área do produto (IA de Negócio, Central de Crescimento,
  Anonymous Auth) foi tocada nesta sessão.

## Próximos passos reais

1. Executar a validação manual real da Auditoria Centralizada em produção:
   alterar pedido e produto de teste, confirmar eventos, ator/tenant,
   ausência de PII e isolamento entre tenants. Só depois disso marcar como
   CONCLUÍDA.
2. Seguir o checklist externo de `docs/WHATSAPP_OFICIAL.md` (App da
   Meta, WABA, número, webhook, secrets, deploy dedicado, piloto) para
   levar o WhatsApp Oficial de "Fase A no código" para conexão real.

## Adendo — mesma sessão, missão seguinte: Chat × IA na main + Anonymous Auth Fase B

Depois do estado acima, a mesma sessão recebeu uma segunda missão com duas
etapas:

**Etapa 1 (concluída, já em `main`)**: integração do commit
`d247cd8` (`fix(loja): coordena chat público e assistente de IA`) —
corrige coordenação entre o widget de chat público e o widget de IA de
Negócio pública (nunca abertos simultaneamente, sem sobrepor o botão
Enviar, restauração idempotente via promise memoizada, mensagens só
renderizam depois da confirmação real do Firestore via
`includeMetadataChanges`). Integrado via fast-forward puro (`git merge
--ff-only`, sem force push); a `main` também havia avançado um commit
(`76276f2`, atualização de status da Auditoria Centralizada, feita fora
desta sessão). Quality Gate confirmado verde (run 30464941383, 4/4 jobs) no
HEAD final `d247cd8`.

**Etapa 2 (código pronto, branch separada, sem merge)**: Anonymous Auth no
Chat Público — Fase B, na branch `feat/anonymous-auth-chat-fase-b`. Remove
o fallback transitório da Fase A (`criarChatLegado()` e
`erroIndicaFallbackTransitorio()` apagados — sem consumidor restante);
`firestore.rules` não aceita mais criar chat V1 novo pelo caminho público
(`chatPublicoValido()` removida do `allow create`); chats V1 que já
existiam continuam funcionando pelo contrato legado de leitura/mensagem/
evento, nunca migrados nem apagados. Ver `docs/ANONYMOUS_AUTH_CHAT_PUBLICO.md`
para o detalhe completo, incluindo os passos externos ainda pendentes antes
do merge (habilitar Anonymous Auth, publicar as Rules, validar um chat real
em produção).

**Nenhum deploy foi executado, Anonymous Auth não foi ativado, nenhuma
credencial foi criada/alterada nesta missão.** WhatsApp Fase B também não
foi iniciada.
