# Handoff — Vide Digital (Vide Hub), sessão de 2026-07-28

Resumo objetivo pra contextualizar outra IA (qualquer uma — Claude, ChatGPT,
Codex, Gemini etc.) sem precisar reler o histórico inteiro do repositório.
Leia isto primeiro, depois `docs/ROADMAP_RD3_STATUS.md` (documento-mestre do
projeto) e `docs/QUALITY_GATE_RELEASE.md` (detalhe do que está descrito
abaixo). Handoff anterior a este: `docs/HANDOFF_2026-07-23.md`.

## O que era o objetivo desta sessão

Fechar de vez o Quality Gate: confirmar `test:ui:login`, `test:ui:flows` e
`test:ui:responsive` 100% verdes num único run real de GitHub Actions, sem
erros de console, sem overflow horizontal e sem teste enfraquecido — e só
então trocar `continue-on-error: true` por bloqueante no job `ui-login`.

Regra explícita seguida: **não assumir que o bloqueio antigo
(`atendimento-templates.flow.mjs`, documentado em
`docs/HANDOFF_2026-07-23.md`) continuava sendo o atual** — rodar o workflow
na `main` atual e tratar a primeira falha real dos logs de agora.

## HEAD inicial

`d533b29` (commit da feature "Pedidos Executivos V1", da sessão anterior,
mesclada logo antes deste ciclo começar).

## O que aconteceu

Ao disparar o Quality Gate contra o HEAD inicial (run `30319890335`), a
primeira falha real dos logs **não era mais** `atendimento-templates.flow.mjs`
— era um bug novo, introduzido pela própria feature "Pedidos Executivos V1"
da sessão anterior:

```
pedidos.flow: FALHOU — page.click: Timeout 30000ms exceeded.
  - waiting for locator('[data-orders-action="save"]')
  - locator resolved to <button ... data-orders-action="save" class="aura-orders-v1-save">
  - <div aria-hidden="true" class="orders-exec-backdrop is-visivel"></div> intercepts pointer events
```

### Classificação: A. Bug real do app

**Causa raiz**: o backdrop do drawer de detalhe (`orders-executive-v1.js`)
era criado como filho único de `document.body`, enquanto o drawer
(`.aura-orders-v1-detail`, `z-index: 61`) fica aninhado dentro de
`#view-pedidos`. Um ancestral entre os dois — `.view-section.active`, que
tem `animation: rdFadeSlide .45s var(--rd-ease)` em `foundation.css` — cria
seu próprio stacking context. Como o backdrop (anexado direto ao `body`) e o
drawer (dentro desse stacking context isolado) pertenciam a ramos diferentes
da árvore de stacking contexts, a comparação bruta de `z-index` entre os
dois deixava de valer — e o backdrop podia renderizar visualmente por cima
do drawer mesmo com número de `z-index` menor. Resultado prático: o clique
real em "Salvar pedido" nunca chegava ao botão, interceptado pelo backdrop.

**Fix** (`orders-executive-v1.js`): o backdrop passou a ser recriado a cada
abertura como **irmão direto de `.aura-orders-v1-detail`**, inserido via
`detalhe.parentElement.insertBefore(bd, detalhe)`, em vez de filho de
`document.body`. Elementos irmãos que compartilham o mesmo pai imediato têm
comparação de `z-index` sempre bem definida pela spec de CSS,
independentemente do que os ancestrais façam — então o drawer (`z-index:
61`) volta a ficar garantidamente acima do backdrop (`z-index: 60`). A
função `garantirBackdrop()` (singleton) foi substituída por
`criarBackdropJuntoDoDetalhe(detalhe)`; `fecharDrawerVisual()` foi
simplificada, já que o nó antigo do backdrop passa a ser descartado
automaticamente junto com o nó antigo do detalhe sempre que o motor
(`orders-engine-v1.js`) troca `content.innerHTML` num `render()`.

Nenhuma linha de `orders-engine-v1.js` foi tocada neste ciclo — o bug e o
fix inteiros ficaram contidos na camada executiva modular, como o desenho
original da feature previa.

**Validação local antes do push**: `node --check orders-executive-v1.js`,
`pnpm run check` (todos os arquivos) e `pnpm run test:unit` (34/34) —
todos verdes. `git fetch origin main` confirmou ausência de commits
concorrentes antes do push.

Commit: `e6a93e1` — `fix(pedidos): backdrop do drawer bloqueava o clique
real em "Salvar pedido"`.

## Resultado: run 100% verde

Run `30321992380` (SHA `e6a93e1`, `main`, disparado automaticamente pelo
push): https://github.com/VideDigital/vide-digital/actions/runs/30321992380

Os 4 jobs, e dentro do job `ui-login` os 3 steps individualmente (não só a
conclusão geral do job/workflow, que fica mascarada por
`continue-on-error`), confirmados `"conclusion":"success"`:

| Job / step | Conclusão |
|---|---|
| Sintaxe e testes unitários | success |
| Firestore Rules, Storage Rules e Functions legadas | success |
| Smoke de frontend (SDK direto, sem navegador) | success |
| **ui-login** → Login real + navegação pelos 3 perfis | **success** |
| **ui-login** → Fluxos profundos (Pedidos, Atendimento, Templates, CRM 360, Base de Conhecimento, Central de IA) | **success** |
| **ui-login** → Responsividade (5 viewports) | **success** |

Ou seja: `atendimento-templates.flow.mjs`, `crm-base-ia.flow.mjs`,
`ia-copilot.flow.mjs` e `responsive.smoke.mjs` — todos os trechos ainda
nunca confirmados desde `docs/HANDOFF_2026-07-23.md` — passaram limpos de
primeira nesta rodada, sem exigir nenhuma outra correção. Nenhum teste foi
enfraquecido (nenhum assert removido, nenhuma troca de `visible` por
`attached` pra esconder bug visual, nenhum `overflow-x: hidden` global
aplicado) — a única mudança de produto neste ciclo foi o fix real acima.

## Fechamento aplicado

1. `continue-on-error: true` removido do job `ui-login` em
   `.github/workflows/quality-gate.yml` — a suíte de UI agora bloqueia o
   merge como qualquer outro job do gate.
2. Comentários do workflow atualizados pra refletir o estado real (verde
   confirmado, bloqueante, com data e referência a este handoff).
3. `docs/ROADMAP_RD3_STATUS.md` — Quality Gate movido de "PARCIAL" pra
   "CONCLUÍDO".
4. `docs/QUALITY_GATE_RELEASE.md` — seção "Limitações reais" e "Como rodar
   no GitHub Actions" atualizadas: a incerteza sobre `test:ui:*` em CI real
   deixou de existir; a única limitação restante é a impossibilidade de
   rodar essas 3 suítes dentro do sandbox de desenvolvimento (bloqueio de
   egress a `www.gstatic.com`, inalterado).
5. Este documento criado.
6. Workflow disparado de novo, já com o job bloqueante, pra confirmar que o
   próprio commit de fechamento também fica verde — ver seção seguinte.

## Riscos remanescentes

- A suíte de UI ainda não cobre tenant cruzado via navegador — coberto,
  com mais precisão, pela suíte de Rules (`firestore-security.test.mjs`),
  decisão já documentada em `docs/QUALITY_GATE_RELEASE.md`.
- Não existe edição completa de um pedido já criado (só criação e mudança
  de status) — limitação de produto real, não de teste, já documentada.
- O sandbox de desenvolvimento continua sem conseguir rodar `test:ui:*`
  localmente; qualquer mudança futura nessas áreas só se confirma de
  verdade via GitHub Actions.

## Rollback exato

Se o job bloqueante causar algum bloqueio inesperado de merge por motivo de
infraestrutura (não de bug real de produto), o rollback é uma única linha:
devolver `continue-on-error: true` ao job `ui-login` em
`.github/workflows/quality-gate.yml` (reverter o commit de fechamento desta
sessão, ou reaplicar a linha manualmente). Nenhuma outra mudança deste ciclo
precisa de rollback — o fix do backdrop é uma correção real e independente
que vale por si só, mesmo que o gate volte a ser informativo.

---

## Segunda missão do mesmo dia: Edição Completa de Pedido Existente V1

Com o Quality Gate já bloqueante (commit `4c9c6be`), a próxima melhoria
pedida foi: permitir editar de verdade um pedido já criado — antes só
status/pagamento/responsável/prazo/desconto/frete/observações internas
eram editáveis; cliente, contato, recebimento, endereço, observações do
cliente, itens e texto livre eram só leitura. Detalhe completo do desenho
em `docs/PEDIDOS_EDICAO_COMPLETA.md`.

Implementado em `orders-engine-v1.js` (modo de edição no detalhe, draft
local, diff real, catálogo sob demanda) e `pedidos-estruturados.js`
(funções puras novas: criarDraftPedido, normalizarDraftPedido,
validarDraftPedido, compararPedidoComDraft, resumirAlteracoesPedido,
atualizarPrecoItem, gerarProdutosTextoSemSobrescreverManual), com seis
campos opcionais novos na whitelist de `pedidos/{id}` em
`firestore.rules`. Todos os testes locais (check, unit, rules,
frontend:emulator) verdes antes de cada push — mas como o gate agora é
bloqueante de verdade, cada push disparava uma run real de ~15-20 minutos,
e o job `ui-login` (com os passos novos escritos pra exercer a edição
completa ponta a ponta em `pedidos.flow.mjs`) revelou, um de cada vez, uma
sequência de bugs reais que só apareciam rodando contra um navegador de
verdade — nenhum deles visível nos testes locais (unit/Rules não tocam
DOM/timing de navegador).

### Bugs reais encontrados e corrigidos, em ordem cronológica

1. **Histórico nunca persistia pra pedidos sem lead vinculado.**
   `persistOrder()` só construía e gravava o array de histórico dentro do
   `if (order.leadId)` — pedidos criados pelo modal "Novo pedido" (a
   maioria dos pedidos internos, sem lead) nunca tinham histórico real,
   só o evento sintético "Pedido registrado". Corrigido: histórico
   calculado incondicionalmente, gravado também em `pedidos/{id}.historico`
   (campo novo, whitelist com teto de 40 itens). Achado ANTES do primeiro
   push, revisando o próprio fluxo do teste.
2. **Diff marcava "alterado" por causa da hora do prazo.** O modal de novo
   pedido grava `prazoEntrega` com hora fixa `T00:00:00`; a edição
   completa reconstrói o mesmo `<input type=date>` com `T12:00:00` (evita
   escorregar de dia por fuso/DST) — mesma data de calendário,
   milissegundo diferente. O diff novo comparava por igualdade exata de
   timestamp, então toda edição nascia "suja" à toa. Fix: comparar
   `dueDate` por dia de calendário, não por milissegundo exato.
3. **Diff marcava "alterado" por regenerar texto customizado à toa.**
   `criarDraftPedido()` sempre começava com `productsTextManual: false`,
   então `normalizarDraftPedido()` regenerava "Produtos ou serviços" a
   partir dos itens assim que a edição abria — mesmo sem o usuário tocar
   em nada. Um pedido com texto customizado (diferente do que os itens
   gerariam sozinhos) nascia "sujo". Fix: detectar a divergência na
   criação do draft e nascer `productsTextManual: true` nesse caso,
   preservando o texto existente.
4. **UI travava em "Salvando..." pra sempre.** `saveEdit()` não chamava
   `render()` depois de limpar `editingId` no sucesso, contando com o
   listener em tempo real (`onSnapshot`) pra refletir o estado — mas não
   há ordem garantida entre o listener disparar e o `await
   batch.commit()` resolver. Quando o listener disparava primeiro, ele
   renderizava ainda com `editingId` setado (modo de edição, já com os
   dados novos) e nada renderizava de novo depois. Fix: `saveEdit()`
   sempre chama `render()` ao final, sucesso ou falha.
5. **Corrida de DOM ao editar itens rapidamente** —
   `NotFoundError: Failed to set the 'outerHTML' property on 'Element'`.
   Dois eventos "change" próximos (quantidade e depois preço) podiam
   tentar substituir o mesmo nó quase ao mesmo tempo. Fix: agendamento via
   `requestAnimationFrame` (mesmo padrão de `orders-executive-v1.js`) +
   checagem `isConnected` antes de substituir.
6. **Preço digitado sumia ao mudar quantidade em seguida** — o fix #5
   (adiar via `requestAnimationFrame`) tinha um efeito colateral real: o
   quadro adiado da mudança de QUANTIDADE recriava o campo de preço via
   outerHTML usando o estado ainda ANTIGO (sem o preço novo, que só vira
   estado quando o próprio "change" do preço dispara) — a troca do nó
   descartava o valor recém digitado. Fix: mudar quantidade/preço nunca
   recria os próprios campos de input — só atualiza os totais calculados
   (linha, subtotal, texto automático) via texto/valor direto.
7. **Botão "Cancelar edição" nunca teve `id`.** Só tinha
   `class="aura-orders-v1-edit-cancel"` — qualquer seletor por id (o
   teste, ou qualquer código futuro) nunca encontrava o botão. Fix:
   adicionado `id="aura-orders-v1-edit-cancel"`. Aproveitado o ciclo pra
   adicionar `autocomplete="off"` nos campos novos de texto (prática
   padrão pra dado de negócio) e blindar `isEditDirty()` com try/catch no
   render da barra de ações e no refresh do selo "não salvo" — nunca deixa
   uma exceção esconder os botões de Cancelar/Salvar.

Sete rodadas de diagnóstico (push → run real de ~15-20min → log real →
causa raiz → fix mínimo → testes locais → push de novo), cada uma
corrigindo exatamente o que os logs mostravam, nunca aumentando timeout
como solução, nunca enfraquecendo o teste (nenhum assert removido, nenhum
`state:"attached"` pra esconder bug visual).

### Run final verde

Run `30358224507` (SHA `53e51a5`, branch `main`): os 4 jobs e, dentro de
`ui-login`, os 3 steps individualmente —
`test:ui:login`/`test:ui:flows` (com os passos novos de edição completa:
abrir edição, editar cliente/recebimento/itens, salvar, fechar/reabrir,
recarregar, confirmar persistência, histórico sem dado sensível, segunda
edição cancelada não persiste, cenário reader)/`test:ui:responsive` —
todos `"conclusion":"success"`.

### Modelo de dados final

Seis campos opcionais novos na whitelist de `pedidos/{id}` em
`firestore.rules`: `clienteWhatsapp`, `clienteEmail`, `tipoRecebimento`
(enum retirada/entrega/não informado), `cep`, `endereco`,
`observacoesCliente` — mais o campo `historico` (lista, teto 40) do bug
#1. Nomes alinhados ao contrato já usado em `pedidoSnapshot` (leads).
`leads` não precisou de nenhuma regra nova (update já não tinha whitelist
de campos). Nenhuma identidade canônica do CRM é tocada pela edição —
apenas o snapshot do próprio pedido.

### Riscos remanescentes (Edição Completa)

- O seed do Quality Gate só tem um produto ativo no catálogo — o teste de
  UI não exercita "adicionar produto novo via busca" de ponta a ponta
  (cobre a busca não encontrando um item já presente, e a função pura
  `adicionarItemPedido` tem cobertura unitária completa à parte).
- O caminho `pedidoSnapshot.*` (pedidos vinculados a lead, via dot-path)
  não tem teste de UI end-to-end dedicado — o pedido do teste é sempre
  legado puro (criado pelo modal). Coberto por teste de Rules dedicado e
  por reaproveitar o mesmo padrão de merge já usado (e testado) para
  `pedidoDesconto`/`pedidoFrete`.

### Rollback (Edição Completa)

Reverter os commits desta feature (do commit que introduz
`orders-executive-v1.js`... não, do primeiro commit desta seção em diante:
`feat(pedidos): adiciona edição completa...` até o commit final que move
esta linha pro CONCLUÍDO). Nenhuma migração de dado é necessária: os
campos novos são opcionais e pedidos antigos continuam funcionando sem
eles — reverter o código simplesmente volta a ignorá-los (dados já
gravados, se houver, ficam órfãos e inofensivos no documento).

---

## Terceira missão do mesmo dia: Anonymous Auth no Chat Público — Fase A

Commit-base confirmado antes de iniciar: `aed3faa8e9c1c7b983959f52c190a9e4b3777158`
(HEAD de `main` no momento; Quality Gate confirmado verde no run
`30360641153`, os 4 jobs, antes de qualquer alteração). Detalhe completo da
arquitetura em `docs/ANONYMOUS_AUTH_CHAT_PUBLICO.md` — esta seção cobre
implementação, testes, run e o que falta pra Fase B.

### Resumo da missão

Substituir "conhecer o id aleatório do chat" como única capability de
acesso ao chat público por identidade real via Firebase Anonymous Auth
(`visitorUid`), sem derrubar o chat durante o rollout (duas fases) e sem
criar Cloud Function nova.

### Implementação (Fase A)

- **`public-chat-auth-core.js`** (novo) — lógica pura, sem import de
  Firebase: chave/serialização/parse da referência local, validação de
  pertencimento (visitante × loja × versão), sanitização do nome,
  normalização de erro e decisão do fallback transitório.
- **`public-chat-auth-v1.js`** (novo) — reexporta o core + app Firebase
  secundário (`"vide-public-chat"`, mesmo `firebaseConfig`, Auth/Firestore
  próprios, Emulators condicionais), `garantirVisitanteAnonimo()` (lazy,
  reaproveita sessão persistida, sem duplicar conta em clique duplo),
  `restaurarChatSalvo()` (fluxo completo de restauração com todas as
  confirmações do mandato).
- **`loja.html`** — integrado ao widget existente (nada reescrito):
  `criarChatV2()`/`criarChatLegado()`/`iniciarConversa()` (tenta V2,
  cai pro legado só em `auth/operation-not-allowed`/`permission-denied`,
  com `console.warn` e comentário `TRANSITÓRIO`), `enviarMensagemCliente()`
  unificada (grava autoria real quando `visitorUidAtual` existe),
  `tentarRestaurarChatSalvo()` disparado ao carregar a vitrine e ao abrir o
  widget, estados de UI (conectando/restaurando/erro) e `escaparHtmlChat()`
  (endurecimento: o banner "Conectado como X" nunca escapava o nome antes
  desta missão — self-XSS de baixo risco, corrigido de graça por já estar
  reescrevendo essa linha).
- **`firestore.rules`** — `isAnonymousAuth()`, `chatTemAcessoV2()`,
  `chatLegadoSemVisitor()`, `visitantePodeAcessarChat()`,
  `chatPublicoV2CreateValido()`, `chatPublicoV2UpdateValido()`,
  `mensagemClienteV2Valida()`, `eventoClienteV2Valido()`. Os caminhos
  legados (`chatUpdatePublicoValido`, `mensagemClienteValida`,
  `eventoAtendimentoClientePublicoValido`) ganharam o guard
  `chatLegadoSemVisitor()` — **um chat que já nasceu V2 nunca mais aceita
  escrita pública sem identidade**, mesmo durante a Fase A (não esperou a
  Fase B, era a única forma de não piorar a segurança do rollout). Leitura
  de `chats/{chatId}` e `mensagens/{msgId}` ganhou o ramo
  `visitantePodeAcessarChat()`; `eventos/{eventoId}` não mudou (visitante
  nunca lê, V1 ou V2).

### Bug real encontrado na auditoria (antes de qualquer código)

O `onSnapshot(doc(db, "chats", salaChatId))` que o widget já usava pra
acompanhar `status` do chat **sempre falhava por permissão** — a regra de
leitura de `chats/{chatId}` sempre exigiu `signedIn()`, e o visitante nunca
tinha sessão nenhuma antes desta missão. Não travava a UI (o `onSnapshot`
não tinha callback de erro, e `chatStatusAtual` já nascia `"nova"` no
cliente), mas o valor nunca era atualizado por essa via — `chatStatusAtual`
só mudava de fato pela lógica local (`statusAntes` no envio de mensagem).
Corrigido de graça pela Fase A: agora que o visitante V2 tem `request.auth`
real, a leitura passa a funcionar de verdade via
`visitantePodeAcessarChat()`.

### Testes locais (todos verdes antes do push)

- `pnpm run check` — sintaxe de todo o projeto, incluindo os 2 arquivos
  novos e o `<script type="module">` de `loja.html` (extraído e checado à
  parte, já que HTML não entra no `node --check`).
- `pnpm run test:unit` — 34 testes na suíte agregada, incluindo os 27 novos
  de `tests/public-chat-auth-v1.test.mjs` (importa `public-chat-auth-core.js`
  diretamente — sem import de Firebase, sem depender de rede).
- `pnpm run test:rules` — **197 testes**, incluindo 24 novos cobrindo:
  criação V2 (visitorUid == auth.uid, versão fixa, whitelist), leitura
  restrita ao próprio visitante, update só por quem tem a identidade,
  imutabilidade de `visitorUid`/`versaoAcesso`, mensagens/eventos com
  autoria real (nunca vazia), e — o mais importante — que os três caminhos
  LEGADOS (update/mensagem/evento) **nunca mais funcionam** uma vez que o
  chat nasceu V2, mesmo sem alteração nenhuma no restante do contrato.
- `pnpm run test:frontend:emulator` — smoke inalterado, incluindo as
  Functions legadas `createPublicChat`/`sendPublicChatMessage` (não
  tocadas por esta missão — usam Admin SDK, não passam pelas Rules
  alteradas; existem à parte, não são chamadas por `loja.html` hoje).
- `pnpm run test:ui:flows` (novo `tests/emulator/ui/loja-chat-publico.flow.mjs`)
  **não pôde ser confirmado localmente** — sandbox de desenvolvimento
  bloqueia `www.gstatic.com` (mesma limitação de sempre, ver
  `docs/QUALITY_GATE_RELEASE.md`). Confiança vem do run real de CI abaixo.

### Teste de UI novo (`loja-chat-publico.flow.mjs`)

Fluxo real: visitante A abre a loja, cria chat V2, troca mensagem, confirma
via Admin SDK que `versaoAcesso`/`visitorUid`/autoria de mensagem+evento
estão corretos; recarrega a página e confirma que a MESMA conversa é
restaurada (mesma sessão anônima persistida); visitante B (contexto de
navegador isolado) nunca herda a conversa — nem forjando a referência local
com o chatId+visitorUid REAIS do visitante A (a sessão anônima da própria
aba de B tem outro uid, então `restaurarChatSalvo()` recusa antes mesmo de
tocar o Firestore); dono responde pelo painel de Atendimento
(`#atend-busca` filtra pelo nome do visitante) e a resposta chega em tempo
real na aba do visitante A, ainda aberta; zero overflow horizontal no
viewport mobile (390×844); zero erro de console relevante nas três
páginas.

### Run de CI (Fase A)

Push feito a partir do HEAD `aed3faa8e9c1c7b983959f52c190a9e4b3777158`, no
commit `e8ae3ae19a884fa8db216637d16de839f3d0e8b1`. O run disparado por esse
commit (`30365281250`) foi **cancelado** — não por falha, mas porque o push
seguinte (`040d7cba9d73fa8f5782925f9d536b22ee072a86`, correção de CSS da
sidebar mobile, sem relação com o chat público) caiu no mesmo
`concurrency.group` do workflow (`cancel-in-progress: true`), que sempre
cancela o run anterior do mesmo branch. Como `040d7cb` é um commit
descendente que contém integralmente o código desta missão, o run que de
fato confirma a Fase A é o dele:

**Run `30366546039`** (SHA `040d7cba9d73fa8f5782925f9d536b22ee072a86`,
branch `main`) — os 4 jobs `success`, e dentro de `ui-login` os 3 steps
individualmente `success`: "Login real + navegação pelos 3 perfis"
(14:05–14:20), "Fluxos profundos" (14:20–14:24, inclui o novo
`loja-chat-publico.flow.mjs`), "Responsividade (5 viewports)"
(14:24–14:26). Este é o run/SHA que fecha a Fase A.

### Riscos remanescentes (Fase A)

- O teste de UI novo depende de seletores reais de `atendimento.js`
  (`#atend-busca`, `[data-atend-conversa-id]`, `#atend-resposta-input`,
  `#atend-form-resposta button[type=submit]`) confirmados por leitura do
  código-fonte (mesmos usados em `atendimento-templates.flow.mjs`), mas
  **não executados localmente** antes do primeiro push — mesma limitação
  de rede de sempre. Se falhar, o diagnóstico segue o mesmo processo já
  documentado (ler o JSON de diagnóstico do artifact/log do job).
- O fallback transitório (Fase A) significa que chats novos em produção
  continuam nascendo V1 até o passo externo ser confirmado — documentado
  em `docs/KNOWN_LIMITATIONS.md`, não escondido.
- `chats/{chatId}` ganhou leitura pro visitante V2
  (`visitantePodeAcessarChat`), o que também corrigiu de graça o bug do
  `onSnapshot` de status (ver acima) — mas só pra chats V2; chats V1
  legados continuam com o mesmo comportamento de antes (o `onSnapshot` de
  status continua falhando por permissão pra eles, como sempre falhou —
  não é regressão desta missão, é o estado herdado).

### Passos externos obrigatórios (antes da Fase B)

Projeto **vide-digital-saas**:

1. Firebase Console → Authentication → Sign-in method → **Anonymous** →
   Enable.
2. Deploy do workflow "Deploy Firebase Spark" (publica `firestore.rules`
   desta Fase A) — sem publicar Functions.
3. Teste real na loja de produção: iniciar um chat, confirmar
   `visitorUid`/`versaoAcesso: 2` no documento, confirmar mensagem com
   `autorUid`, responder pelo painel, confirmar que o visitante recebe.
4. Confirmar que a sessão do dono continua intacta abrindo a loja pública
   na mesma aba/origem em que ele está logado no dashboard (app secundário
   isolado — não deveria nunca ter afetado, mas é o teste real que fecha a
   garantia).

**Só depois dessa confirmação** a Fase B (remover o fallback legado, negar
`create` de chat V1 novo, publicar Rules finais) deve começar — nunca antes.

### Rollback (Fase A)

Reverter os commits desta missão (do primeiro commit `feat(chat-publico):
prepara Anonymous Auth...` em diante) restaura o estado anterior. Nenhum
dado precisa ser apagado: chats V2 eventualmente já gravados em produção
continuam com `visitorUid`/`versaoAcesso` órfãos e inofensivos (campos
opcionais, nenhum código antigo os lê). Anonymous Auth pode continuar
habilitado no Console sem problema mesmo depois de um rollback de código —
ou ser desativado depois, à parte. Em incidente: nunca usar
`allow read/write: if true`, nunca remover os checks de tenant, nunca
migrar chats no susto — a prioridade é restaurar criação/envio do chat
(o fallback legado já cobre isso automaticamente caso a causa seja
Rules/Anonymous ainda não publicados).
