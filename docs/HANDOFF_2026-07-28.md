# Handoff — Vide Digital (Vide Hub), sessão de 2026-07-28

Resumo objetivo pra contextualizar outra IA (qualquer uma — Claude, ChatGPT,
Codex, Gemini etc.) sem precisar reler o histórico inteiro do repositório.
Leia isto primeiro, depois `docs/ROADMAP_RD3_STATUS.md` (documento-mestre do
projeto) e `docs/QUALITY_GATE_RELEASE.md` (detalhe do que está descrito
abaixo). Handoff anterior a este: `docs/HANDOFF_2026-07-23.md`.

## O que era o objetivo desta sessão

Fechar de vez o Quality Gate: confirmar `test:ui:login`, `test:ui:flows` e
`test:ui:responsive` 100% verdes num único run real de GitHub Actions, sem
erros de console, sem overflow horizontal e sem teste enfraquecido — e só
então trocar `continue-on-error: true` por bloqueante no job `ui-login`.

Regra explícita seguida: **não assumir que o bloqueio antigo
(`atendimento-templates.flow.mjs`, documentado em
`docs/HANDOFF_2026-07-23.md`) continuava sendo o atual** — rodar o workflow
na `main` atual e tratar a primeira falha real dos logs de agora.

## HEAD inicial

`d533b29` (commit da feature "Pedidos Executivos V1", da sessão anterior,
mesclada logo antes deste ciclo começar).

## O que aconteceu

Ao disparar o Quality Gate contra o HEAD inicial (run `30319890335`), a
primeira falha real dos logs **não era mais** `atendimento-templates.flow.mjs`
— era um bug novo, introduzido pela própria feature "Pedidos Executivos V1"
da sessão anterior:

```
pedidos.flow: FALHOU — page.click: Timeout 30000ms exceeded.
  - waiting for locator('[data-orders-action="save"]')
  - locator resolved to <button ... data-orders-action="save" class="aura-orders-v1-save">
  - <div aria-hidden="true" class="orders-exec-backdrop is-visivel"></div> intercepts pointer events
```

### Classificação: A. Bug real do app

**Causa raiz**: o backdrop do drawer de detalhe (`orders-executive-v1.js`)
era criado como filho único de `document.body`, enquanto o drawer
(`.aura-orders-v1-detail`, `z-index: 61`) fica aninhado dentro de
`#view-pedidos`. Um ancestral entre os dois — `.view-section.active`, que
tem `animation: rdFadeSlide .45s var(--rd-ease)` em `foundation.css` — cria
seu próprio stacking context. Como o backdrop (anexado direto ao `body`) e o
drawer (dentro desse stacking context isolado) pertenciam a ramos diferentes
da árvore de stacking contexts, a comparação bruta de `z-index` entre os
dois deixava de valer — e o backdrop podia renderizar visualmente por cima
do drawer mesmo com número de `z-index` menor. Resultado prático: o clique
real em "Salvar pedido" nunca chegava ao botão, interceptado pelo backdrop.

**Fix** (`orders-executive-v1.js`): o backdrop passou a ser recriado a cada
abertura como **irmão direto de `.aura-orders-v1-detail`**, inserido via
`detalhe.parentElement.insertBefore(bd, detalhe)`, em vez de filho de
`document.body`. Elementos irmãos que compartilham o mesmo pai imediato têm
comparação de `z-index` sempre bem definida pela spec de CSS,
independentemente do que os ancestrais façam — então o drawer (`z-index:
61`) volta a ficar garantidamente acima do backdrop (`z-index: 60`). A
função `garantirBackdrop()` (singleton) foi substituída por
`criarBackdropJuntoDoDetalhe(detalhe)`; `fecharDrawerVisual()` foi
simplificada, já que o nó antigo do backdrop passa a ser descartado
automaticamente junto com o nó antigo do detalhe sempre que o motor
(`orders-engine-v1.js`) troca `content.innerHTML` num `render()`.

Nenhuma linha de `orders-engine-v1.js` foi tocada neste ciclo — o bug e o
fix inteiros ficaram contidos na camada executiva modular, como o desenho
original da feature previa.

**Validação local antes do push**: `node --check orders-executive-v1.js`,
`pnpm run check` (todos os arquivos) e `pnpm run test:unit` (34/34) —
todos verdes. `git fetch origin main` confirmou ausência de commits
concorrentes antes do push.

Commit: `e6a93e1` — `fix(pedidos): backdrop do drawer bloqueava o clique
real em "Salvar pedido"`.

## Resultado: run 100% verde

Run `30321992380` (SHA `e6a93e1`, `main`, disparado automaticamente pelo
push): https://github.com/VideDigital/vide-digital/actions/runs/30321992380

Os 4 jobs, e dentro do job `ui-login` os 3 steps individualmente (não só a
conclusão geral do job/workflow, que fica mascarada por
`continue-on-error`), confirmados `"conclusion":"success"`:

| Job / step | Conclusão |
|---|---|
| Sintaxe e testes unitários | success |
| Firestore Rules, Storage Rules e Functions legadas | success |
| Smoke de frontend (SDK direto, sem navegador) | success |
| **ui-login** → Login real + navegação pelos 3 perfis | **success** |
| **ui-login** → Fluxos profundos (Pedidos, Atendimento, Templates, CRM 360, Base de Conhecimento, Central de IA) | **success** |
| **ui-login** → Responsividade (5 viewports) | **success** |

Ou seja: `atendimento-templates.flow.mjs`, `crm-base-ia.flow.mjs`,
`ia-copilot.flow.mjs` e `responsive.smoke.mjs` — todos os trechos ainda
nunca confirmados desde `docs/HANDOFF_2026-07-23.md` — passaram limpos de
primeira nesta rodada, sem exigir nenhuma outra correção. Nenhum teste foi
enfraquecido (nenhum assert removido, nenhuma troca de `visible` por
`attached` pra esconder bug visual, nenhum `overflow-x: hidden` global
aplicado) — a única mudança de produto neste ciclo foi o fix real acima.

## Fechamento aplicado

1. `continue-on-error: true` removido do job `ui-login` em
   `.github/workflows/quality-gate.yml` — a suíte de UI agora bloqueia o
   merge como qualquer outro job do gate.
2. Comentários do workflow atualizados pra refletir o estado real (verde
   confirmado, bloqueante, com data e referência a este handoff).
3. `docs/ROADMAP_RD3_STATUS.md` — Quality Gate movido de "PARCIAL" pra
   "CONCLUÍDO".
4. `docs/QUALITY_GATE_RELEASE.md` — seção "Limitações reais" e "Como rodar
   no GitHub Actions" atualizadas: a incerteza sobre `test:ui:*` em CI real
   deixou de existir; a única limitação restante é a impossibilidade de
   rodar essas 3 suítes dentro do sandbox de desenvolvimento (bloqueio de
   egress a `www.gstatic.com`, inalterado).
5. Este documento criado.
6. Workflow disparado de novo, já com o job bloqueante, pra confirmar que o
   próprio commit de fechamento também fica verde — ver seção seguinte.

## Riscos remanescentes

- A suíte de UI ainda não cobre tenant cruzado via navegador — coberto,
  com mais precisão, pela suíte de Rules (`firestore-security.test.mjs`),
  decisão já documentada em `docs/QUALITY_GATE_RELEASE.md`.
- Não existe edição completa de um pedido já criado (só criação e mudança
  de status) — limitação de produto real, não de teste, já documentada.
- O sandbox de desenvolvimento continua sem conseguir rodar `test:ui:*`
  localmente; qualquer mudança futura nessas áreas só se confirma de
  verdade via GitHub Actions.

## Rollback exato

Se o job bloqueante causar algum bloqueio inesperado de merge por motivo de
infraestrutura (não de bug real de produto), o rollback é uma única linha:
devolver `continue-on-error: true` ao job `ui-login` em
`.github/workflows/quality-gate.yml` (reverter o commit de fechamento desta
sessão, ou reaplicar a linha manualmente). Nenhuma outra mudança deste ciclo
precisa de rollback — o fix do backdrop é uma correção real e independente
que vale por si só, mesmo que o gate volte a ser informativo.
